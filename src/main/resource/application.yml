server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://192.168.1.90/task8db
    username: postgres
    password: masterkey
  luquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml

app:
  limit:
    reset-cron: 0 0 0 * * *